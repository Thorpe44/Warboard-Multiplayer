WARBOARD - REMOVE FAILED V54 TERRAIN BOOTSTRAP

Removes ONLY the runtime terrain-overhaul files from the V54 patch that produced
the bad screenshot.

It does not roll back the game build, rules, current terrain implementation,
mission logic, placement ghost, multiplayer code, or build number.

Run:
REMOVE_FAILED_V54_TERRAIN_BOOTSTRAP.bat
